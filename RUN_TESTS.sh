#!/bin/bash

# Скрипт для запуска тестов с автоматическим определением параметров подключения

echo "🚀 Запуск тестов для Food Delivery Service"
echo ""

# Определяем имя пользователя
if [[ "$OSTYPE" == "darwin"* ]]; then
    # macOS
    DB_USER=$(whoami)
    DB_PASSWORD=""
    echo "📍 Обнаружена macOS, используем пользователя: $DB_USER"
elif [[ "$OSTYPE" == "linux-gnu"* ]]; then
    # Linux
    DB_USER="postgres"
    DB_PASSWORD="postgres"
    echo "📍 Обнаружен Linux, используем пользователя: $DB_USER"
else
    DB_USER="postgres"
    DB_PASSWORD="postgres"
    echo "📍 Используем пользователя по умолчанию: $DB_USER"
fi

# Проверяем подключение
echo "🔍 Проверка подключения к БД..."
if ! ./check_db_connection.sh > /dev/null 2>&1; then
    echo "❌ Ошибка: База данных не настроена!"
    echo "   Запустите: ./check_db_connection.sh"
    exit 1
fi

echo ""
echo "🧪 Запуск тестов..."
echo ""

# Запускаем тесты
mvn test -Ddb.user="$DB_USER" -Ddb.password="$DB_PASSWORD"

EXIT_CODE=$?

echo ""
if [ $EXIT_CODE -eq 0 ]; then
    echo "✅ Все тесты прошли успешно!"
else
    echo "❌ Некоторые тесты не прошли. Проверьте вывод выше."
fi

exit $EXIT_CODE

